#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);   //desynchronizes C++ standard streams with that of C
    cin.tie(NULL);      // Unguarantees the flushing of cout stream before the next cin
    return 0;
}